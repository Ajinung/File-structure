import React from "react";

const Loading = () => {
  return (
    <center
      style={{
        width: "100%",
        height: "100vh",
        backgroundColor: "#000000c0",
        color: "#fff",
      }}
    >
      Loading
    </center>
  );
};

export default Loading;
